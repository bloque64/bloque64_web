/* import { Store } from 'vuex'
import { getModule } from 'vuex-module-decorators'
import discussion_store from '~/store/discussions_store'

let discussionStore: discussion_store

function initialiseStores(store: Store<any>): any {
  return getModule(store)
}

export { initialiseStores, discussionStore } */
